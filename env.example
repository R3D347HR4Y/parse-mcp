# Parse Server MCP Configuration
# Rename this file to .env and fill in your values
# Or set these as environment variables directly

# =============================================================================
# Parse Server Connection
# =============================================================================

# Required: Your Parse Server URL
# Examples:
#   - Back4App: https://parseapi.back4app.com
#   - Self-hosted: https://your-domain.com/parse
#   - Local: http://localhost:1337/parse
PARSE_SERVER_URL=https://parseapi.back4app.com

# Required: Your Parse Application ID
PARSE_APP_ID=your-app-id

# Recommended: Master Key for admin operations
# Required for: schema access, config updates, aggregation
# ⚠️ WARNING: Master Key bypasses all security - use carefully!
PARSE_MASTER_KEY=your-master-key

# Optional: JavaScript Key
PARSE_JS_KEY=

# Optional: REST API Key
PARSE_REST_KEY=

# =============================================================================
# MCP Transport Configuration
# =============================================================================

# Transport mode: "http" (default) or "stdio"
# - http: Runs an HTTP server, ideal for Docker/remote deployment
# - stdio: Uses stdin/stdout, ideal for local IDE integration (Cursor, Claude Desktop)
MCP_TRANSPORT=http

# HTTP server port (only used in HTTP mode)
MCP_PORT=3000

# HTTP server host (only used in HTTP mode)
# Use 0.0.0.0 to listen on all interfaces (required for Docker)
MCP_HOST=0.0.0.0

